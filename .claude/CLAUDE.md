# `.claude/CLAUDE.md` — sibling pointer

The canonical project memory lives at the repository root: [`../CLAUDE.md`](../CLAUDE.md).
Claude Code auto-loads it at the start of every session. This file exists
only so the root `CLAUDE.md` line *"treat the two as the same document"*
matches reality.

Open `.claude/README.md` for the agent operating manual (rules, history,
reports, guides, commands, hooks, agents, skills).
