# `.claude/commands/` — Custom slash-commands

Currently empty. When a slash-command would help (e.g. `/run-april`,
`/compare-contrasts`), add a `<command>.md` here with the prompt body.

User invokes via `/<command-name>` from any session in this repo.
