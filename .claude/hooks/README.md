# `.claude/hooks/` — Custom hooks

Currently empty. Hooks fire on events (pre-tool-use, post-tool-use,
session-start, etc.) and run a shell command.

Configure via `.claude/settings.json` (or `settings.local.json`) under
the `hooks` key. Document the rationale of each hook here.
